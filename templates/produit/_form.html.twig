{{ form_start(form, {'attr': {'class': 'form'}}) }}
    <div class="mb-3">
        {{ form_row(form.nom) }}
    </div>
    <div class="mb-3">
        {{ form_row(form.description) }}
    </div>
    <div class="mb-3">
        {{ form_row(form.prix) }}
    </div>
    <div class="mb-3">
        {{ form_row(form.fdsFile) }}
    </div>
    <button class="btn btn-primary">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
