{% extends 'base.html.twig' %}

{% block title %}Fds{% endblock %}

{% block body %}
    <h1 class="animated fadeInLeft">Fds</h1>

    <table class="table table-striped table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fd.id }}</td>
            </tr>
            <tr>
                <th>Chemin_pdf</th>
                <td>{{ fd.cheminPdf }}</td>
            </tr>
            <tr>
                <th>Creer_le</th>
                <td>{{ fd.creerLe ? fd.creerLe|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Supprimer_le</th>
                <td>{{ fd.supprimerLe ? fd.supprimerLe|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_fds_index') }}">back to list</a>

    <a href="{{ path('app_fds_edit', {'id': fd.id}) }}" class="btn btn-warning">edit</a>

    {{ include('fds/_delete_form.html.twig') }}
{% endblock %}
