{% extends 'base.html.twig' %}

{# ... #}

{% block body %}
    <div class="container mt-5">
        {{ form_start(form, {'attr': {'class': 'col-md-6 offset-md-3'}}) }}
        
        {{ form_widget(form) }}
        
        <button type="submit" class="btn btn-primary">{{ button_label|default('Valider') }}</button>
        
        {{ form_end(form) }}
        
        <p class="mt-3">Déjà inscrit ? <a href="{{ path('app_login') }}">Connectez-vous</a></p>
    </div>
{% endblock %}
